export declare global {
    interface Window {
        isWide?: boolean
        hasBlackSide?: boolean
        setSize?: function
        vd?: {
            aid?: number
            bvid?: string
        }
    }
}
